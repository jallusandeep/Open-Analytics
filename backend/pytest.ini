[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers --html=reports/test-report.html --self-contained-html
markers =
    unit: isolated business logic tests without Docker
    integration: real API and DuckDB tests requiring Docker
